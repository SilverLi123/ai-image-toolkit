# 项目 4：AI 图片工具箱 (AI Image Toolkit)

## 项目概述

一个基于浏览器的图片处理工具集，集成 AI 能力，提供图片压缩、格式转换、批量水印、AI 去背景等功能。所有处理在浏览器本地完成（Canvas API），AI 功能通过 API 调用。

## 目标用户

- 自媒体创作者（需要快速处理图片）
- 电商运营（批量加水印、压缩产品图）
- 设计师（快速去背景、格式转换）

## 功能需求

### 必做功能（MVP）

1. **首页 — 工具选择**
   - 卡片式展示所有工具
   - 每个工具有图标、名称、简述
   - 点击进入对应工具页面

2. **工具 1：图片压缩**
   - 拖拽或点击上传图片（支持 JPG/PNG/WebP）
   - 质量滑块（10%-100%）
   - 实时预览压缩前后对比（左右分栏）
   - 显示原始/压缩后文件大小
   - 一键下载压缩结果
   - 支持批量上传（最多 10 张）

3. **工具 2：格式转换**
   - 上传图片后选择目标格式（JPG ↔ PNG ↔ WebP）
   - 预览 + 文件大小对比
   - 批量转换 + 批量下载（ZIP 打包）

4. **工具 3：批量水印**
   - 上传图片
   - 水印文本输入（自定义文字）
   - 水印位置选择（9 宫格点击选择）
   - 透明度滑块
   - 字体大小滑块
   - 实时预览水印效果
   - 批量添加 + 下载

5. **工具 4：AI 去背景**
   - 上传图片
   - 调用 AI API 去除背景
   - 显示去背景前后对比
   - 下载透明背景 PNG

6. **通用功能**
   - 暗色/亮色模式
   - 上传进度条
   - 处理中动画
   - 错误提示（文件过大、格式不支持等）

### 加分功能

- 图片裁剪工具
- AI 图片增强（超分辨率）
- 拖拽排序批量图片顺序
- 处理历史记录
- 自定义水印图片（上传 Logo 作为水印）

## 页面结构

```
单页应用
├── 首页（工具选择）
├── 图片压缩
├── 格式转换
├── 批量水印
└── AI 去背景
```

## 设计要求

- **布局：** 顶部导航 + 主内容区，工具页为左右分栏（左侧设置/右侧预览）
- **风格：** 现代、干净、工具感，类似 Canva / TinyPNG 的感觉
- **配色：** 白色为主 + 品牌色渐变（如蓝紫渐变）
- **拖拽区域：** 虚线边框、图标提示、hover/active 状态变化
- **预览区：** 棋盘格背景（显示透明区域）
- **对比滑块：** 左右拖拽对比原图和处理后效果

## 技术约束

- 纯 HTML/CSS/JS
- 图片处理全部使用 **Canvas API**（浏览器本地，无需后端）
- AI 去背景使用 API（如 remove.bg API 或 GLM 视觉模型）
- ZIP 打包使用 JSZip（CDN 引入）
- 文件大小限制：单张 10MB
- 不使用任何前端框架

## 项目结构

```
04-ai-image-toolkit/
├── index.html
├── css/
│   ├── variables.css
│   ├── base.css
│   ├── navbar.css
│   ├── home.css          # 首页工具卡片
│   ├── upload.css        # 上传区域
│   ├── preview.css       # 预览/对比区域
│   ├── controls.css      # 滑块/按钮控件
│   └── responsive.css
├── js/
│   ├── router.js         # 页面路由
│   ├── theme.js          # 主题切换
│   ├── upload.js         # 拖拽上传/文件选择
│   ├── compress.js       # 图片压缩（Canvas toBlob quality）
│   ├── convert.js        # 格式转换
│   ├── watermark.js      # 批量水印（Canvas drawText）
│   ├── remove-bg.js      # AI 去背景 API 调用
│   ├── compare.js        # 前后对比滑块
│   ├── download.js       # 下载/批量 ZIP 打包
│   └── utils.js          # Canvas 辅助函数
└── assets/
    └── icons/            # 工具图标 SVG
```

## 实现顺序（建议按此顺序开发）

```
第 1 步：首页 + 拖拽上传组件
  → 工具选择首页（卡片式）
  → 通用拖拽上传组件（所有工具复用）
  → 验证：拖拽文件能显示预览

第 2 步：图片压缩工具（最核心的工具，先做）
  → Canvas toBlob 质量压缩
  → 前后对比滑块
  → 文件大小显示
  → 下载按钮
  → 验证：压缩后文件明显变小，画质可接受

第 3 步：格式转换工具
  → 复用压缩逻辑，改变 MIME type
  → 批量 + ZIP 打包
  → 验证：JPG→PNG→WebP 互转正确

第 4 步：批量水印工具
  → Canvas fillText 绘制水印
  → 9 宫格位置选择
  → 透明度/大小滑块
  → 验证：水印效果自然，批量处理正常

第 5 步：AI 去背景
  → API 集成 + 棋盘格透明背景显示
  → 验证：去背景效果可接受

第 6 步：暗色模式 + 响应式 + 错误处理
  → 全站暗色模式
  → 移动端适配
  → 文件过大/格式不支持的友好提示
  → 验证：各种边界情况处理正确
```

## 设计参考

| 网站 | 参考点 | 链接 |
|------|--------|------|
| TinyPNG | 图片压缩工具 UI 参考 | https://tinypng.com/ |
| Squoosh | Google 出品的图片压缩工具，对比滑块参考 | https://squoosh.app/ |
| Canva | 工具首页卡片式布局参考 | https://www.canva.com/ |
| remove.bg | AI 去背景工具 UI 参考 | https://www.remove.bg/ |
| iLoveIMG | 多功能图片工具集参考 | https://www.iloveimg.com/ |

## 踩坑指南

| 坑 | 解决方案 |
|----|---------|
| Canvas toBlob 在 JPEG 透明区域变黑 | 先 `fillRect` 白色背景再 drawImage |
| drawImage 图片没画上去 | 确保在 `img.onload` 回调内执行 Canvas 操作 |
| 大图片压缩导致卡顿 | 用 `setTimeout` 分批处理，或用 Web Worker |
| Safari 不支持 WebP | 用 `canvas.toBlob(cb, 'image/webp')` 前检查浏览器支持 |
| 对比滑块拖拽在手机端不跟手 | 监听 `touchmove` 而非 `mousemove`，用 `e.touches[0].clientX` |
| 批量处理内存溢出 | 每处理完一张就 `URL.revokeObjectURL()` 释放内存 |
| ZIP 文件名中文乱码 | JSZip 使用 UTF-8 编码，文件名避免特殊字符 |
| 拖拽上传后文件顺序不对 | 用 `DataTransferItemList` 获取原始顺序 |

## Vibe Coding 提示词模板

```
请帮我实现一个 [具体工具名] 功能。

技术要求：
- 使用 Canvas API 处理图片
- 支持拖拽上传和点击上传
- 处理完成后可下载结果
- 批量模式支持最多 10 张图片

功能细节：
[描述具体功能]

请给出完整的 HTML + CSS + JS 代码，包含上传区域、预览区域和下载按钮。
```

## 验收标准

- [ ] 拖拽上传正常工作，支持多文件，有文件大小验证
- [ ] 图片压缩可调节质量，文件大小明显减小
- [ ] 前后对比滑块拖拽流畅
- [ ] 格式转换输出正确格式，批量 ZIP 打包正常
- [ ] 水印位置、透明度、大小可调，预览实时更新
- [ ] AI 去背景返回透明背景 PNG，有棋盘格背景显示
- [ ] 批量处理有进度条显示
- [ ] 下载文件名有意义（如 "compressed_photo.jpg"）
- [ ] 移动端可正常使用
- [ ] 文件过大（>10MB）或格式不支持时有友好提示
